server {
  listen 80;
  server_name bootstrap.adafruit.com;

  root /var/www;
  index  index.txt;

  add_header X-Powered-By biko;

  location / {
    log_not_found off;
    access_log off;
    try_files $uri $uri/ =404;
  }
}
